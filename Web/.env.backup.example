# ==================================================
# KEEPER - SERVER PROD (FALLBACK/BACKUP)
# ==================================================
# 
# Este archivo se usa automáticamente si la BD principal falla
# 
# IMPORTANTE: 
# 1. Copiar este archivo como .env.backup (sin .example)
#    cp .env.backup.example .env.backup
# 
# 2. Configurar con credenciales de servidor de PRODUCCIÓN
# 3. NO SUBIR .env.backup al repositorio (protegido por .gitignore)
#
# FLUJO DE FAILOVER:
# 1. .env (primaria - Server Dev o Local Dev)
# 2. .env.backup (respaldo - Server Prod) ← ESTE ARCHIVO
# 3. Si ambas fallan → Exception clara con detalles

# ============ ENTORNO ============
APP_ENV=prod

# ============ RUTAS BASE ============
APP_BASE_PATH=/Repo_2026/keeper
APP_BASE_URL=/Repo_2026/keeper/public

# ============ BASE DE DATOS ============

# --- SERVER PROD (producción) ---
DB_HOST=production-server.mylogin.co
DB_NAME=pipezafra_keeper_prod
DB_USER=pipezafra_keeper_prod
DB_PASS=YOUR_PRODUCTION_PASSWORD_HERE

# --- SERVER PROD ALTERNATIVO (si tienes múltiples servers) ---
#DB_HOST=backup-server.mylogin.co
#DB_NAME=pipezafra_verter
#DB_USER=pipezafra_verter
#DB_PASS=YOUR_BACKUP_PRODUCTION_PASSWORD

# ============ CHARSET ============
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# ============ API ============
# Si tu API queda bajo /dominio/keeper/public/api
API_PREFIX=/api
