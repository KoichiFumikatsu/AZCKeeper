node_modules/

# Credenciales
.env
.env.backup
.env.production
.env.local

# SQL dumps (legacy DB son enormes — no versionar).
# Excepción: migraciones del backend PHP SÍ se versionan.
*.sql
!AZCKeeper_Client/Web/migrations/*.sql

# C# Build Artifacts
bin/
obj/
*.user
*.suo
build/

# Visual Studio (índices Copilot, caché de IDE — nunca versionar)
.vs/

# Paquetes ZIP — siempre artefactos, nunca fuente. Los builds van a build/
# y los paquetes deploy PHP ad-hoc (web.zip, webold.zip) se generan a demanda.
*.zip

# Logs
*.log

# OS
.DS_Store
Thumbs.db
desktop.ini
*~
*.swp

# Graphify — grafo de conocimiento generado (artefacto, se regenera con `graphify update .`)
graphify-out/
